import styled from "styled-components";

const GoogleCallback = () => {
  return (
    <Wrapper>
      <h1>GoogleCallback body</h1>
    </Wrapper>
  );
};

const Wrapper = styled.div``;
export default GoogleCallback;
